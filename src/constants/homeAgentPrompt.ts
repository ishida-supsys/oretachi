/**
 * ホームワークツリーで管理エージェントを起動するときの既定プロンプト。
 *
 * 定型操作の実体は `<worktreeBaseDir>/.claude/skills/` のスキル側に置いてあるので、
 * ここは「スキルを見に行かせる」ことと「削除には承認を要求する」ことだけを伝える短いものにする。
 * 設定 `homeAgentPrompt` で上書きできる。
 */
export const DEFAULT_HOME_AGENT_PROMPT = `あなたはワークツリー追加先ディレクトリを拠点に、配下のワークツリー群を管理するエージェントです。

.claude/skills/ に定型操作（棚卸し・状況レポート・タスク割り当てなど）が定義されています。
まず利用可能なスキルを確認し、指示を待ってください。

ワークツリーの削除は必ず候補を提示してユーザーの承認を得てから実行してください。`;
