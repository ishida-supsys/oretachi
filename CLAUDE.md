# oretachi — リポジトリ共通の作業ルール

## auto-merge (`automerge` ラベル)

**`automerge` ラベルの付いた PR は、CI (`type-check` / `cargo-check`) が通り次第、
人手を介さず自動でマージされます。**
`.github/workflows/enable-auto-merge.yml` が `gh pr merge --auto --merge` を代行するためです。
必須チェックは main ruleset の CI 2件だけで、**レビュー承認は要求されません**。

### 自動で有効化される条件

* draft でない **かつ** `automerge` ラベルが付いている PR。
* 発火契機は `opened` / `reopened` / `ready_for_review` / `labeled` の4つ。

### 有効化・解除の非対称性（重要）

* **有効化は最初の1回だけ**です。`synchronize` は拾っていないため、
  auto-merge が解除された状態（人が手で切った / コンフリクトで GitHub 側が解除した等）は
  push しても再武装されません。
  再武装したいときはラベルを外して付け直します（`labeled` が再発火します）。
* **ラベルを外しても auto-merge は解除されません。**
  ラベルは起動契機にすぎず、GitHub 側の auto-merge フラグとは独立です。
  止めるには `gh pr merge --disable-auto <PR>` を実行するか、
  PR 画面の Disable auto-merge を押します。

### AI が守ること

`automerge` ラベルは「人の承認が済んだ印」ではなく、
**「この PR に人の判断を要する論点はもう残っていない」という宣言**です。
したがって AI が自分で付けても構いませんが、**次の条件をすべて満たすことが前提**です。

* セルフレビューを実施し、挙がった指摘を解消済みであること。
* 該当する変更を含む場合、`pnpm run type-check` と `cd src-tauri && cargo check` が
  ローカルで通っていること。
* 差分が issue / 依頼のスコープに収まっており、想定外の副作用がないこと。
* **この auto-merge の仕組み自体を弱める変更でないこと。** 具体的には
  `.github/workflows/enable-auto-merge.yml`、main ruleset やブランチ保護、リリース関連、
  権限・シークレット、そしてこの `CLAUDE.md` の規約そのものに触る変更です。
  ゲートの定義を変える PR を、そのゲートを通り抜けてマージしてはいけません。
* **人・issue・依頼元のスキルがレビューを求めていないこと。** 誰かが明示的にレビューを
  要求しているなら、AI 自身に迷いがなくても条件は満たしていません。
* **人に聞きたいことが1つも残っていないこと。** 例えば次のいずれかに当てはまるなら残っています。
  * 仕様の解釈が割れ、読み方次第で成果物が変わる
  * 妥当な代替案があり、どちらを採るかで迷っている
  * 後方互換性やデータ移行に影響する
  * UI 文言・デザインなど、好みが判断に入る
  * 単に、レビューで意見が欲しい

**1つでも満たせない、あるいは迷ったらラベルを付けずに出してください。**
draft で出す場合も同様にラベルは付けません。draft はラベルが付いていれば
`ready_for_review` で auto-merge が有効化されるため、それ自体は歯止めになりません。

理由は取り返しのつき方が逆だからです。ラベルの付け忘れは後から人がいつでも付けられますが、
誤って付けた PR は CI が通った瞬間にマージされ、取り消せません。判断に迷ったら
「付けない」側へ倒します。

なお**セルフレビューの具体的な手段（コマンド名・スキル名）は環境ごとに異なる**ため、
ここには書きません。各自の環境設定（`.claude/CLAUDE.md` など、リポジトリに含まれない
ローカル側の指示）に従ってください。

ラベルを付ける場合・すでに付いている場合の注意:

* **ラベルが付いた PR は「出してから直す」ができません。**
  CI が通った瞬間にマージされるため、CI エラー以外の理由
  （レビュー指摘、自分で気づいた修正漏れ、コミットメッセージの直し等）で
  後から追いコミットする猶予はありません。
  → PR を作る前に修正を完了させること。レビューを挟みたい場合は
  ラベルを付けずに出すか、draft で出してください。
* すでに auto-merge が有効な PR で修正が必要になったと気づいた場合は、
  追いコミットの前に **`gh pr merge --disable-auto <PR>` で止める**
  （ラベルを外すだけでは止まりません）。
  すでにマージ済みだった場合は追加 PR で直します。
